# where the per-category folders live
TORRENT_DATA_ROOT=Z:/Torrent/vod_files

# HTTP listen address
LISTEN=:4001

# optional cache housekeeping
CACHE_EVICT_TTL=168h           # drop idle torrents from session after 7 days
CACHE_MAX_BYTES=200000000000   # ~200 GB across all categories (best-effort)