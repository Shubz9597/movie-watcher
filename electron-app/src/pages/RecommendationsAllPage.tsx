// RecommendationsAllPage route entry (M4.2): the shared See-all view lives in
// components/shared/RecommendationRow.tsx; this page module exists so the
// lazy route loader keeps the pages/ convention.
export { RecommendationsAllPage as default } from '../components/shared/RecommendationRow';
