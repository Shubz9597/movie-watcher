App/build
App/Pods
App/output
App/App/public
DerivedData
xcuserdata

# Cordova plugins for Capacitor
capacitor-cordova-ios-plugins

# Generated Config files (config.xml is a REQUIRED static template file --
# it must stay version-controlled or fresh clones fail to build: keep tracked)
