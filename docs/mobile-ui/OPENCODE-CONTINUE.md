# OpenCode prompt: close BFF prerequisites, continue mobile alpha

Paste this into OpenCode at the current movie-watcher workspace. It supersedes starting blindly at Phase 9 or assuming phases 1–8 are complete.

---

Continue torWatch development from the existing working tree. First read `docs/mobile-ui/PREREQUISITE-STATUS.md`, feature 001's `spec.md`, `plan.md`, `tasks.md`, contracts and evidence, and `.specify/memory/constitution.md`. Then read `docs/mobile-ui/OPENCODE-PROMPT.md`, `architecture.md`, `change-map.md`, and feature 002's spec/plan/data-model/tasks. Follow these existing artifacts directly; do not rerun specification generation or replace their tasks with a new generic plan.

The goal is a verified shared BFF foundation followed by the shared React mobile alpha, iPhone first and Android next. The owner accepted the current mobile interaction diagrams for alpha only; later visual refinement remains open. Reuse the Electron components and the Go BFF; no separate native UI rewrite.

**Do not assume phases 1–8 are complete.** The checked-in/current task ledger has T020, T034, T042–T044, T050 and T059–T064 open. Re-read current files because another agent may have advanced them. The audit is a starting point, not permission to ignore newer evidence. Preserve all pre-existing dirty/untracked work and record the exact baseline; do not reset, clean, force-checkout, or commit unrelated work wholesale.

1. Reproduce the available baseline: Electron tests/renderer build, Go tests/vet, and dependency availability. Resolve local cache/tooling issues safely. Do not label a setup failure as a code regression or skipped DB/native tests as a pass. Capture command, working directory, exit result and environment limits.
2. Complete the software gaps in the existing BFF migration before declaring it a mobile foundation. Inspect real requests and imports in TitlePage, SeeAllPage, continue-service, catalog-gateway and catalog-bff. The default remains `renderer`; BFF mode still has documented legacy detail/genre/enrichment dependencies. Characterize them, add missing versioned server behavior and contract tests, migrate the consumers, and keep existing renderer mode for rollback. Add child tasks to feature 001 for discovered gaps rather than concealing them in unrelated mobile tasks. Verify movies, series, anime, multi-season episodes, sources, subtitles and resume.
3. Run T020/T034/T042/T050 and phase exits where the environment supports them. Use an isolated disposable DB/stack for migrations, ordered progress, shared leases, capacity, gateway ranges/SSE, scripts and rollback. Cover the current migration set including 006, not only the older 005 drill. Never use production volumes/data for tests. If Docker/DB or interactive hardware is unavailable, prepare reproducible commands and report the exact blocked evidence while continuing independent code/fixture work.
4. Flip the default to BFF only after T042 and its recorded acceptance requirements are satisfied; complete T043/T044 with tests and smoke. Review the previously documented parity/progress decisions against existing authority and actual behavior. If a material unresolved user decision remains, present the concrete difference and required decision after completing independent implementation/verification; do not invent approval or repeatedly ask about routine choices.
5. Treat Phase 9 as gated retention/cleanup/release work, not mandatory immediate deletion. Prepare per-path decision records now. Keep renderer-provider code/CSP paths until the full-release-cycle/parity/approval gate actually passes. Keep `/subtitles/configure`: it has a live setup caller, despite its stale inclusion in T067. Keep `/watch/*` and `/stats`; inspect every other candidate's current consumers. Do not mark deferred deletion as completed deletion. Do not cut/publish a release or claim a Radxa soak/race/rollback pass without evidence. Missing Phase 9 cleanup alone must not block isolated feature 002 work.
6. Once the necessary software baseline is established, execute feature 002's M0, adapter/browser-safe M1.1–M1.2 and independent M2–M4 work using `OPENCODE-PROMPT.md`. Reuse current baseline evidence rather than rerunning identical tests unnecessarily, but verify again after relevant changes. While native/Radxa gates are pending, shared UI may use explicitly isolated fixtures and APIs may be tested locally. Do not enable a dependent unverified persistence/playback path or declare its integration complete. Keep the hardware-dependent M1/M5/M6 gates open until actual device evidence exists.
7. For mobile UI, use the five revised wireframe boards, design-system and visual-qa workflow. Build real shared components, capture phone/desktop states, inspect the images, fix concrete issues, and recapture. Measure scrolling, animation and input response. Preserve bare icons, the Continue carousel, collection/media Library hierarchy and readable torrent rows. No fake screenshots or duplicated mock application.

Maintain two separate task ledgers: feature 001 for existing BFF/verification/release work; feature 002 for mobile/library/recommendations. Mark a task complete only with its required evidence. Keep blocked hardware and deferred cleanup visible in reports. Use current workspace tools available to OpenCode; no Codex plugin/tool assumptions. A missing optional skill is not a blocker when these docs provide the requirements.

You may execute this prompt directly without Spec Kit slash commands. Keep the spec/plan/tasks/contracts and constitution available in the workspace. If invoking scripts, set `SPECIFY_FEATURE_DIRECTORY` explicitly to the feature being worked on; saved feature selection may point elsewhere. Avoid changing another agent's saved feature selection in a shared checkout.

Proceed into the authorized implementation; do not stop after repeating this plan. At each stopping point return completed user-visible outcomes, exact tasks closed, changed files, test/capture/measurement evidence, remaining failures or blocked hardware, retained legacy paths and reasons, rollback status, and the next task. Do not publish, submit to app stores, or expose the private server publicly.

---

Acceptance of this assignment authorizes development and verification, not fabricated release evidence or unconditional removal of still-used compatibility code.
