# Prompt for the implementation agent

For OpenCode use [OPENCODE-PROMPT.md](OPENCODE-PROMPT.md), which includes portable tooling instructions and feature selection. The owner accepted the current wireframes for alpha on 2026-09-06, while leaving later visual refinement open. Read [architecture.md](architecture.md), [change-map.md](change-map.md), and the feature's data-model.md in addition to the documents below.

Implement torWatch feature `002-mobile-shared-ui`, starting with M0 and following the dependency order in `specs/002-mobile-shared-ui/tasks.md`. Reuse the Electron React components for a fast, smoothly animated phone experience. iPhone comes first, Android next. Use thin mobile shells rather than a separate native UI rewrite; validate the proposed Capacitor/player choice in M1. Shared source changes must reach all clients through their builds, while household library state syncs through the Version 2 BFF.

Read `PRODUCT.md`, `DESIGN.md` and `docs/mobile-ui/design-system.md`, `.specify/memory/constitution.md`, the feature's `spec.md`, `plan.md`, `tasks.md`, and `docs/mobile-ui/wireframes.md` / `visual-qa.md`. Open the SVG/PNG boards visually. Consult feature 001 contracts and current source for existing behavior; earlier handoff status text and completed checkboxes are not runtime evidence.

Root PRODUCT.md and DESIGN.md are currently gitignored and may be absent in another checkout. In that case use `docs/mobile-ui/README.md` and `design-system.md` for the preserved product/design context, then verify against source. Do not block merely because those local root copies are missing.

Use the installed Impeccable skill for adaptation and visual review, and the applicable Go skills for backend changes. If using Spec Kit automation, explicitly resolve feature 002 before running scripts: current branch inference may point at feature 001. Run consistency analysis, then implement; preserve the server feature's tasks and all unrelated dirty work. Do not rewrite the legacy Next.js frontend or repurpose the root V1 Compose file.

Deliver Watch Later and Favourites as independent server-persisted household memberships, plus the bounded explainable recommendation rules in plan.md. Do not treat deviceId as a user/auth identity. No provider secret in a client bundle, local-only library fork, or silent BFF-to-provider fallback.

Characterize before refactoring, define/test new contracts before migrating consumers, keep each checkpoint runnable, and record tests plus rollback. Split oversized tasks before coding. Isolate incomplete features. Continue work that does not depend on hardware while Radxa/Mac/iPhone evidence is pending. Mark only proven tasks done; do not certify iPhone playback using MPV success or browser emulation. Add necessary mobile transport work explicitly if M1 finds it is required; do not hide it in UI changes.

For each UI phase, run actual application components with deterministic fixtures, capture the required phone and Electron/desktop states, open the images, identify defects, fix them in one batch, and recapture once to verify. Test keyboard, touch, accessibility, failed saves, and Back behavior. Measure animation, scrolling, and input latency; screenshots alone cannot establish smoothness. Follow the bounded review and report template in `visual-qa.md`. Native release evidence must come from the built simulator/device app, with real-device playback evidence.

The revised five wireframe boards supersede the initial pill-based drafts. Use bare icon tools for Back/Settings/Close/save, with accessible names and full touch areas. Continue Watching is a horizontal carousel: artwork resumes; title opens detail; dragging must never launch playback. Library has underlined Watch Later/Favourites tabs, then distinct Movies/Series/Anime shelves leading to scoped full grids. Home categories use an open icon rail; Search filters live in a sheet. Torrent rows show source metadata and separate selection, details, and explicit Play. Do not restore the old full-width Resume bar, repeated Open title links or capsule-shaped navigation controls.

Do not publish, submit to stores, or expose the homeserver publicly. Return changed files, verified user outcomes, command and image evidence, numeric performance results, unresolved/blocked checks, rollback status, and the next task. Never describe missing device verification as a pass.
