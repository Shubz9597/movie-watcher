# M2.3 reconciliation + M2.4 evidence — motion, images, cancellation, measurement

Executed: 2026-09-06. Closes the M2.3 follow-ups, then implements M2.4 per tasks.md and the plan.md performance contract. All results below are DEVELOPMENT measurements (headless Chromium on the host, 390×844@2x mobile emulation) — they are NOT iPhone/Android acceptance results; device performance acceptance remains pending.

## M2.3 reconciliation

1. **WF06a source-details disclosure** (TorrentPanel): per-row "Details" toggle (`aria-expanded`/`aria-controls`) opens a bounded panel with the full release name, quality, size (human + bytes), seeders/leechers, indexer, publish date, info hash, magnet availability — every missing field rendered as explicit "Unknown", never invented. "Use this torrent" confirms the selection, closes the details, returns focus to the row's toggle (list position preserved — the list is not remounted), and never starts playback (a visible note says so).
2. **Interaction verification (real browser)**: details open → metadata/hash present → confirm → details closed, focus restored to the toggle, selection kept, hash unchanged (no navigation). Refresh with a changed fixture id set → selection cleared with the "no longer available" notice. ALL PASS (see capture run log lines `[interaction] PASS`).
3. **SelectionSurface lifecycle**: the open-state effect now runs only when `open` flips; the latest `onClose` is read through a ref, so parent rerenders or new callback identities never reset focus. Additionally the GlobalSearch filter sheet was hoisted OUT of the results conditional — a loading/empty transition (which previously remounted the sheet) no longer drops focus. Verified: focus stays on the sheet option while new results arrive, and Escape returns focus to the opener.
4. **Honesty updates**: tasks.md M2.3/M2.4 rows updated with this evidence; plan.md's stale "implementation not started" status replaced with the actual phase state, explicitly noting that no hardware/playback gates have passed.

## M2.4 implementation

- **Shared route motion**: `AppShell` wraps routed content in a keyed container with `torwatch-route-in` (opacity + 10px translateY, `var(--motion-route)`/`--ease-standard`); animates opacity/transform only — never layout properties; reduced motion collapses the duration in CSS. Route state, scroll restoration, and immediate input feedback unchanged (measured below).
- **Sheet enter/exit motion**: SelectionSurface now plays `torwatch-sheet-out`/`torwatch-backdrop-out` on close before unmounting, with the close delay read from the `--motion-sheet` token so reduced motion closes immediately.
- **Image audit**: PosterCard, ContinueCarousel, HomePage rail and Library grids already set intrinsic `width`/`height` (no CLS), `loading="lazy"` offscreen, `decoding="async"`; posters use w342 (correct for 2× phone rails); hero uses `fetchPriority="high"` eager. Library fixture stress cards intentionally render without artwork to keep the measurement workload network-free. No changes required; audit recorded.
- **Cancellation/late-response audit** (search, title, episodes, sources): GlobalSearch uses requestId guards + debounce; HomePage/SeeAllPage/TitlePage use cancelled flags/AbortController; EpisodePanel's torrent fetches use a request-id sequence; catalog-bff aborts tracked requests and discards responses across origin switches; TorrentPanel refresh is single-flight and its responses are abort-tracked. No unguarded late-response path found; no changes required.
- **Pending/error feedback**: search (loading/empty/error/retry), episodes (season + torrent states), sources (loading/empty/error/notice), player (truthful failure copy) — all present from earlier tasks; no gaps found.

## Measurement (same documented fixtures, before vs after)

Harness: `scripts/measure-m24.mjs` (`npm run measure:m24 -- <label>`), serving the real `dist-browser` build with explicit fixtures; long tasks via PerformanceObserver, frame gaps via a 1ms-threshold rAF sampler; raw JSON in `evidence/m24-metrics/before-m23.json` and `after-m24.json`.

Environment: development Chromium (puppeteer headless), 390×844 @2x mobile emulation, no network throttling, fixture data (0 artwork requests in the scroll workload), host Windows machine. NOT a device result.

| Scenario | Before | After |
|---|---|---|
| Route switch (bottom nav) input→paint | 18 ms | 13 ms |
| Library tab switch input→paint | 7 ms | 5 ms |
| Filter sheet open input→dialog | 5 ms | 5 ms |
| 10 s scroll, 100 titles | 0 long tasks, 0 gaps>32 ms | 0 long tasks, 0 gaps>32 ms |

Targets (plan.md): input feedback next frame / ≤100 ms — met; no repeatable main-thread task >50 ms during the 10-second 100-title scroll — met (0 long tasks); frame budget — no >32 ms gap observed. All values are development measurements; device acceptance stays open.

## Reduced-motion verification

With `prefers-reduced-motion: reduce` emulated: computed sheet animation-duration collapsed to ~0 ms and Escape closed the sheet promptly. PASS (`[interaction] reduced-motion`).

## Captures

All 16 captures re-run on the final build and inspected (home, library, category, title anime/movie, source details/selected, search sheet, failure gates, live degraded/switch). No animation-induced layout or capture regressions found; the route-enter animation completes before capture settle.

## Tests / builds

- `npm test` 81/81; `tsc --noEmit` clean; `npm run build:renderer` PASS (Electron dist intact); `npm run build:browser` PASS; feature 001 Go suites unaffected this task (last run PASS incl. Origin-null regressions).

## Remaining gaps / M2.5 handoff

- M2.5: full `visual-qa.md` matrix pass (320px stress, landscape, 200% text, keyboard-occlusion states), batched defect/fix/recapture report with the template's field set, and `evidence/visual-followups.md`.
- Performance traces remain development measurements; iPhone/Android acceptance pending real devices (M5/M6).
- Live Electron MPV regression after the PlayerPort refactor still needs the operator run (T020/T042 checklist); no native playback claim is made.
- Library persistence (M3) still gated on the identity-collision contract work; save actions remain disabled-truthful.
