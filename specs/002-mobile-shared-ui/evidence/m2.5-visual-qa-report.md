# M2.5 visual-QA report (bounded browser pass) + M2.4 evidence correction

Phase: feature 002 M2.5 | Source revision: HEAD `bd11a90a` + the recorded dirty working tree (see `git status`; build identity in m24 JSON) | Date: 2026-09-06

## M2.4 evidence correction (recorded first)

The earlier `evidence/m2.4-motion-performance.md` reported only long-task counts and >32 ms gaps and claimed the plan's targets "met" from single samples. Corrected:

- **Corrected harness**: `scripts/measure-m24.mjs` now records EVERY rAF interval in each workload (total frames, median/p95/max interval, **percentage within the 16.7 ms budget**), keeps >32 ms gaps as an additional metric, separates startup from steady-scroll windows, asserts the **scroll container actually moves** (`scrollVerified` — run fails on a static page), measures **visible destination state** (Home hero heading / Favourites underline / sheet dialog box — not URL change), repeats every scenario 3×, adds a **cached-artwork workload** (10 harness-served 1×1 PNGs, cache-warmed), and saves a **CDP performance trace** (`evidence/m24-metrics/corrected-m24-trace.json.gz`).
- **Corrected results** (3 repeats each; `corrected-m24.json`):
  - scroll100_text: frames 3252/3236/3203, median 2.8 ms, p95 5.5–5.6 ms, max ≤6 ms, **100 % within 16.7 ms**, 0 gaps>32 ms, 0 long tasks, scrollVerified true — all runs.
  - scroll100_artwork (cached placeholders): frames 3236/3237/3170, identical summary — 100 % within 16.7 ms, 0/0.
  - routeSwitch (input→visible hero): 110 / 30 / 29 ms — run 1 includes the Home lazy chunk fetch (startup effect separated from steady state); steady state ≈30 ms.
  - tabSwitch (input→underline moved): 52 / 5 / 5 ms (first run re-rendered Favourites content); sheetOpen: 5 / 4 / 3 ms.
- **Correction of claims**: "targets met" is withdrawn as stated; the corrected statement is: *in development Chromium, ≥95 % of frames fell within 16.7 ms with zero long tasks across all repeats, measured via DOM/rAF proxies with a saved CDP trace*. Frame-interval sampling in headless Chromium is not vsync-locked (median 2.8 ms < 16.7 ms) — presentation evidence is the trace, and **native acceptance remains open** (M5/M6). Environment recorded in the JSON: browser version, host CPU/cores/RAM, HEAD + dirty-file count, viewport, no-throttle conditions.

## Build and tests

| Check | Result |
|---|---|
| `npm test` | PASS 81/81 |
| `npm run build:renderer` / `build:browser` | PASS / PASS (Electron dist intact) |
| `tsc --noEmit` | clean |

## Capture index

All captures: `evidence/captures/m25/` (29 PNGs from the final build) + `index.json` (full list incl. interaction results). Screens: `home-*` at 320×568, 375×667, 390×844, 430×932, 360×800, 844×390 landscape, 768×1024, 1440×900; `library-*`/`title-*` at 320/390/768/1440; `search-sheet-*` at 320/390/1440; `text200-{home,library,title}`; `reduced-motion-{home,sheet}`; `stress100-library-category`; `carousel-{start,middle,end}`; `simulated-keyboard-focus-search`.

## Visual findings (severity, location, owner)

| ID | Severity | Where | Finding | Disposition |
|---|---|---|---|---|
| V-1 | low | `home-320x568` | Feature hero title (4.5rem token) dominates the 320 px viewport; composition differs from WF01 proportions | Follow-up (visual refinement, token tweak in M2.4+ scope) — recorded in visual-followups.md |
| V-2 | low | `text200-title-390x844` | Compact Back icon button renders slightly taller than wide at 200 % text (aspect drift) | Follow-up (primitive sizing under text scale) |
| V-3 | low | `home-844x390` | Landscape phone uses the desktop header; bottom destinations hidden | Follow-up (landscape shell decision; pointer-first is usable) |
| V-4 | info | `reduced-motion-sheet` | Close button shows a strong focus ring at capture time (programmatic focus) | Accepted — visible focus is required behavior |
| V-5 | info | `simulated-keyboard-focus-search` | Viewport does not resize — **simulated keyboard only**; no native IME in headless Chromium | Native keyboard occlusion verified only on devices (M5/M6) |

## Interaction evidence (real browser, printed by the harness)

- swipe-vs-tap: 8-step pointer drag released ON the artwork → no resume intent, no navigation; plain artwork tap → truthful resume notice. PASS.
- Library Back restoration: shelf → View all → scroll → Back → tab (Watch Later) and scroll position restored (clamped to the Library page's max scroll). PASS after two fixes: (1) the category page's Back dropped the `collection` param — now `navigate('library', { collection })`; (2) scroll re-apply fought fast user scrolls and restored-before-data — replaced with programmatic-scroll tracking + bounded re-apply that yields to the user.
- WF06a details → "Use this torrent" → focus restored to the list, selection kept, no navigation: PASS. Refresh losing the selected source → selection cleared with the visible explanation: PASS.
- Sheet focus containment/persistence/return + tabs keyboard nav + reduced-motion collapse: PASS (see M2.3/M2.4 evidence and harness logs).

## Coverage

- **Passed**: browser viewport matrix (8 widths), 200 % text, reduced motion, 100-title long grid, missing-artwork fallback, carousel start/middle/end, swipe-vs-tap, library tabs/shelves/back restoration, torrent selection/details/disappearance/footer, sheets + focus + pending/error/unsupported states.
- **Failed**: none functional in this pass (initial back-restoration defect was fixed and recaptured in the same run).
- **Blocked**: Electron-native captures (MPV/titlebar/focus) — needs the interactive operator run (T020/T042); iOS/Android captures and real keyboard occlusion — M5/M6 devices; real provider artwork — needs `TMDB_API_KEY` + egress.

## Release gate: shared-browser M2

- M2.1–M2.4 complete with evidence; M2.5 bounded browser pass complete with zero open functional/accessibility defects (three low-severity visual follow-ups recorded). **The shared-browser M2 gate PASSES at the alpha bar** — explicitly scoped: fixture/browser evidence only; desktop Electron regression check (T020/T042) and all native/device gates remain open and untouched.
- Rollback: each M2 task reverts independently (per tasks.md); renderer default and fallback routes untouched.
