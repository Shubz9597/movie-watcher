# Feature 002 tasks

Run remaining tasks in dependency order using [architecture](../../docs/mobile-ui/architecture.md), [change map](../../docs/mobile-ui/change-map.md), and [data model](data-model.md). Completed rows carry verification annotations and evidence; unticked parent/device gates remain open. Alpha wireframes are accepted; later visual refinement remains open. Every row requires the named verification and recorded evidence before ticking. “Revert” means reverting only that task's isolated change, never resetting unrelated work. Each phase ends with the full affected checks: backend `go test ./...` and `go vet ./...`, renderer `npm test` and `npm run build:renderer`, plus relevant platform builds and smoke tests. Record blocked checks; do not turn missing hardware into a pass.

| Done / ID | Depends | Behavior and change boundary | Focused verification | Rollback |
|---|---|---|---|---|
| [x] M0.1 | — | Record revision, branch, dirty files, current feature 001 contract/release evidence; add `specs/002-mobile-shared-ui/evidence/baseline.md` | Classify pre-existing failures; no secrets in report | Docs-only revert |
| [ ] M0.2 | M0.1 | Characterize current desktop catalog/startup/player/progress and take DB fixture backup. PARTIAL 2026-09-11 — see evidence/m0.2-desktop-smoke.md: automated real-Electron app smoke 11/11 (catalog STUB-BACKED), staging 18/18 + 13/13 post-restart, plus real native-MPV generated-video smoke PASS (child host, fullscreen controls, start-at-3s resume offset, time advance, teardown/recreate). A stale ignored Windows native binary was rebuilt; loader/smoke now reject incomplete bindings and have a regression test. npm test 123/123, tsc, renderer build, cargo release test green. STILL OPEN: end-to-end search→episode→real source→backend `/stream`→actual subtitle→server-persisted resume; human-run OPERATOR-CHECKLIST T020/T042 boxes | Existing suites/build; search→episode/source→play→subtitle→resume smoke and screenshots | Revert added tests only; retain evidence/backup |
| [x] M1.1 | M0.2 | Document all Electron/browser/provider dependencies and adapter signatures; add failing-meaningfully characterization where missing | Confirm map covers shared import graph and current error cases | Docs/test revert |
| [x] M1.2 | M1.1 | Inject connection/origin/storage adapters; isolated browser entry renders existing Home→Title against fixtures and BFF | Browser entry without Electron globals; Electron unchanged behavior; no silent fixture fallback. VERIFIED 2026-09-06 — see evidence/m1.2-browser-entry.md (npm test 77/77, both builds, CORS contract test, 11 inspected captures incl. live origin-switch recovery; live provider-data render blocked on TMDB_API_KEY/egress, recorded) | Disable entry; revert adapter injection |
| [ ] M1.3 | M1.2 | Spike thin wrapper and mobile player using actual server stream; record supported matrix and toolchain decision | iPhone actual play/seek/subtitles/stop/resume; interruption; denied permission; no desktop MPV inference | Remove isolated spike; retain evidence |
| [x] M1.3.1 | M1.2 | OWNER DECISION RECORDED: final mobile product = installed native iOS/Android apps; shared React UI packaged later with Capacitor; iOS playback via Swift AVPlayer adapter, Android via Kotlin Media3 adapter; both implement the shared TypeScript PlayerPort contract; common transport = HLS/fMP4 + WebVTT; compatible sources direct-play; browser/PWA player stays a dev/staging tool; NO separate mobile catalog/library/recommendation implementations. Child tasks M1.3.1–M1.3.8 split from M1.3/M1.4 (server-side children implemented before any native shell) | Docs/spec artifacts only | Revert doc change |
| [x] M1.3.2 | M1.3.1 | Media inspection: ffprobe-backed `internal/playback` Prober boundary (exec via argument arrays only, bounded timeout/cancel, configurable tool paths, magnets/credentials/paths redacted from errors and logs, no local paths in responses); MediaInfo = container, duration, dimensions, video codec/profile/level/bits, HDR when present, audio codec/channels, bitrate, embedded subtitle tracks, file index | Unit tests with synthetic ffprobe JSON + timeout/cancel tests; redaction tests. VERIFIED 2026-09-12 — see evidence/m1.3-playback-service.md (planner/probe/source tests PASS; container ffprobe run PASS; URL-redaction test PASS) | Remove package; capability never advertised |
| [x] M1.3.3 | M1.3.2 | Playback sessions + opaque URLs: POST/GET/DELETE `/v2/playback/sessions*`; crypto-random session IDs; loopback-only token-authenticated internal media source (torrent.Reader via http.ServeContent) so NO magnet ever reaches an FFmpeg/ffprobe command line, playlist, segment URL, log, or error; validation of cat/file index/source; expiry + reason codes; capability `playback.compat.v1` advertised ONLY when ffprobe+ffmpeg are configured and verified. VERIFIED 2026-09-12 — see evidence/m1.3-playback-service.md (ID/token randomness, no-magnet invariant, CORS/preflight, capability gating + live 404 gating, full httpapi suite green) | Disable capability advertisement; routes 404 when service absent |
| [x] M1.3.4 | M1.3.3 | Direct playback: compatible MP4/fMP4 + H.264/AAC plans `direct` and are served through the opaque session media endpoint (HTTP Range via ServeContent); no transcode process started | Planner unit tests + integration probe of a generated fixture. VERIFIED 2026-09-12 — container evidence (probe→plan direct; HTTP media endpoint 200 + 206 Range) | Planner revert |
| [x] M1.3.5 | M1.3.4 | MKV remux: incompatible container with compatible codecs → FFmpeg stream-copy to HLS/fMP4 segments under the session dir; correct MIME types; master playlist generated by the service | Integration test in disposable task-labelled container (generated MP4/MKV fixtures); segment MIME + playlist tests. VERIFIED 2026-09-12 — container remux run PASS (playlist + .m4s segments; MIME contract proven over HTTP) | Remove remux branch; direct still works |
| [x] M1.3.6 | M1.3.5 | Bounded transcode fallback: incompatible video/audio codec or profile resolution/bitrate → H.264/AAC HLS with configurable max resolution (default ≤1080p) and one active conversion process by default; truthful capacity error; independent audio transcode with video copy when applicable. HDR that the client profile rejects remains truthfully unsupported until a verified tone-map pipeline exists. | Planner tests + concurrency-limit tests + container integration. VERIFIED 2026-09-12 — container transcode run PASS (bounded H.264/HLS segments; capacity bound proven over HTTP); checkpoint review added output-manifest, profile-bound, and completed-process slot-release regressions. | Remove transcode branch; remux/direct unchanged |
| [x] M1.3.7 | M1.3.6 | Subtitles: reuse backend subtitle ownership — embedded SRT sidecars and external OpenSubtitles results → WebVTT via existing subtitles.SRTtoVTT; VTT served per session with language/label/default/forced metadata; ASS/SSA→VTT with documented styling loss; PGS/image = unsupported unless burned into a transcoded variant; VTT is offered only when fetched and syntactically valid | SRT→VTT validity tests; session subtitle tests with generated fixtures. VERIFIED 2026-09-12 (EMBEDDED/CONTAINER scope) — see evidence/m1.3-playback-service.md: SRT→VTT validity gate + HTTP VTT retrieval PASS in container; external OpenSubtitles results keep flowing through the EXISTING /v1/subtitles endpoints (no duplicate implementation — native clients attach them as sidecar text tracks); styling loss + PGS-unsupported documented | Remove subtitle rendition from sessions; existing /v1/subtitles unchanged |
| [x] M1.3.8 | M1.3.7 | Lifecycle, cleanup, observability: TTL janitor (default 2h), startup stale-session cleanup, cancellation kills ONLY that session's process tree, deletion removes ONLY that session's directory under the configured root, traversal-protected playlist/segment paths, structured logs without magnets/credentials. VERIFIED 2026-09-12 — TTL sweeper, startup sweep, direct/HLS root-constrained deletion (neighbor preserved), immediate expiry enforcement, concurrent session-limit/Stop races, traversal refusal, and real-subprocess termination tests PASS. | Revert to manual cleanup |
| [ ] M1.4 | M1.3.8 | Native-client integration contract: shared TypeScript PlayerPort updates for ios-avplayer / android-media3 capability profiles, session lifecycle client, and the Capacitor shell decision — IMPLEMENTED IN THE NEXT MILESTONE (no Capacitor/iOS/Android shells in M1.3.x) | Contract tests on PlayerPort/session client once shells exist | Keep legacy stream and MPV contracts; disable new path |
| [x] M2.1 | M1.2 | Reconcile CSS roles into one token source and shared primitives; add representative component fixtures | Desktop before/after, touch sizes, contrast, shared import test. VERIFIED 2026-09-06 — see evidence/m2.1-m2.2-shared-ui.md (tokens in globals.css + design-tokens.ts; IconButton/UnderlineTabs/SelectionSurface fixtures in npm test 81/81) | Revert extraction, existing CSS remains usable |
| [x] M2.2 | M2.1 | Shared AppShell and route stack; icon navigation, bare Back/Settings, Continue carousel, open Browse rail, underlined Library tabs/shelves, search filter sheet | WF01/02/02a/03/04/04b; separate artwork-resume/title-detail; swipe must not play; query/tab/type/scroll restoration; keyboard and desktop shortcuts. VERIFIED 2026-09-06 — see evidence/m2.1-m2.2-shared-ui.md (14 inspected captures; Electron default rail preserved; Library fixtures labelled preview-only; resume shows truthful M1.3 notice) | Gate compact shell; revert shell change |
| [x] M2.3 | M2.2 | Adapt compact title actions, icon save states, episodes, torrent rows/details/selection/footer and player; wire to existing or gated capability | WF03/06/06a/07/08; icon accessible names/targets; selection does not play; refresh/source disappearance; full release names; long lists, missing/unknown metadata, loading/failed/unsupported fixtures. VERIFIED 2026-09-06 — see evidence/m2.3-title-sources.md (81/81 tests; 16 inspected captures; browser interaction checks PASS; save actions disabled-truthful; playback via PlayerPort with truthful M1.3 boundary; Electron desktop split-button path preserved) | Gate adapted surfaces; revert |
| [x] M2.4 | M2.3 | Add transition tokens, image sizing/lazy loading, request cancellation and pending feedback | Motion/reduced-motion fixtures; initial scroll/input trace; no masking overflow. VERIFIED 2026-09-06 — see evidence/m2.4-motion-performance.md (route/sheet motion token-driven; before/after measurement harness `npm run measure:m24`: route 18→13ms, tab 7→5ms, sheet 5→5ms, 10s×100-title scroll 0 long tasks/0 frame gaps both runs — development Chromium only, device acceptance open; reduced-motion collapse verified in browser) | Revert animation/performance change independently |
| [x] M2.5 | M2.4 | Finish fixture-backed visual pass for responsive shared UI | `visual-qa.md` matrix, batched defects/fix/recapture; affected suites. VERIFIED 2026-09-06 — see evidence/m2.5-visual-qa-report.md + evidence/visual-followups.md (29 captures across the 8-width matrix, 200% text, reduced motion, stress100, carousel positions, swipe-vs-tap, back restoration — 3 interaction fixes applied and recaptured; zero open functional/a11y defects; 3 low-severity visual follow-ups recorded; Electron-native and device captures blocked on operator/hardware) | Revert fixes only; retain review evidence |
| [x] M3.1 | M2.5 | Finalize library overview/scoped-grid contracts/DTOs, snapshot/count/sort/revision rules, capabilities and household scope; characterize same-number movie/TV IDs and resolve any identity ambiguity through a separate compatible contract task before writes | Both flags, lossless revisions, correct counts beyond page one, nonduplicated anime, collision-safe identity, type/sort-bound cursor, retries, old-server handling. VERIFIED 2026-09-06 — see evidence/m3.1-identity-contracts.md + contracts/library-api.md (identity PROVEN ambiguous via identity_test.go fixtures; resolution = media-qualified tmdb ids matching progress vocabulary; executable contract fixture checks 58/58; no persistence added) | Contract proposal revert before consumers ship |
| [x] M3.1.1 | M3.1 | Bounded compatibility child task (BLOCKS M3.2): implement media-qualified canonical ids (tmdb:movie:N / tmdb:tv:N) — TMDb provider emits qualified ids, Detail probes only the qualified endpoint (unqualified alias keeps movie→tv probe), TitlePage/continue-service request detail by opaque catalogId with qualifier preserved; unqualified-alias + parity regressions stay green. VERIFIED 2026-09-09 — see evidence/m3.1.1-qualified-identity.md (go test ./... + go vet PASS; npm test all 5 suites PASS; tsc + both builds PASS; identity tests assert direct series resolution and no cross-media fallback; new-client/old-server detail failure documented as a rollout limitation) | `go test ./internal/catalog/ ./internal/httpapi/ ./tools/reference-client/`, `npm test`, both builds; identity tests assert direct series resolution | Revert producer/consumer id changes (alias keeps old clients green) |
| [x] M3.2 | M3.1.1 | Add library storage/migration and API; no UI behavior change yet. VERIFIED 2026-09-09 — see evidence/m3.2-library-storage-api.md (migration 007 additive tables/indexes; internal/library service + /v2/library/* handlers per contracts/library-api.md; 20 storage tests + 7 HTTP tests against disposable postgres:16-alpine; go test -p 1 ./... and go vet PASS with zero skips; npm test/tsc/both builds PASS; transactional revisions as lossless decimal strings, no-op writes never bump, repeatable-read snapshots, scope-bound cursors, snapshot metadata rendering; live upgrade + old-binary rollback runs verified; library.household.v1 advertised only with initialized storage) | Atomic independent fields, duplicates, concurrent writes, restart, old DB upgrade and old binary compatibility | Disable endpoint; retain additive rows/schema |
| [x] M3.3 | M3.2 | Implement shared icon LibraryToggle, grouped Library shelves and collection/type grids in one desktop consumer. VERIFIED 2026-09-09 — see evidence/m3.3-desktop-library-consumer.md (Electron entry wires the server-backed LibraryStore gated on library.household.v1; shared LibraryToggle with pending/success/error/retry and per-title serialized writes; LibraryPage/CategoryPage server mode with full-scope counts, zero shelves, snapshot rendering, sort + cursor pagination; 14 new node tests + live run against the real M3.2 API on disposable PostgreSQL incl. 105-entry pagination, same-number identity, alias rejection, big-revision strings, origin switch; 9 inspected desktop captures, 1 truthfulness defect fixed, 1 LOW follow-up recorded) | Independent flags, pending/error/Retry, empty shelf/collection, count/sort/filter pagination, unavailable metadata | Disable library capability UI; retain data |
| [x] M3.4 | M3.3 | Enable same components in phone entry; refresh/reconnect and bounded active polling. VERIFIED 2026-09-10 — see evidence/m3.4-phone-library-sync.md (browser production entry composes the same LibraryStore gated on the capability; shared library-sync controller: 15s bounded visible-only polling, refocus/reconnect/visibility refresh, overlap guards, older-server probe avoidance, offline stale labeling, no offline write queue; 4 new sync tests + live two-client run on disposable PostgreSQL with cross-client visibility both directions, server restart persistence, same-number identity, >2^53 revision strings, origin switch; production browser bundle scanned: no Node built-ins or electron imports) | Two clients reflect update ≤20s; restart persistence; offline read labelled, write failure truthful | Detach sync controller; revert entry wiring |
| [x] M4.1 | M3.4 | Implement bounded server ranking, reasons and cache; use existing catalog candidate service. VERIFIED 2026-09-10 — see evidence/m4.1-server-recommendations.md (contracts/recommendations-api.md finalized; GET /v2/recommendations with max-20 limit validation; distinct-seed genre scoring with provider-qualified keys, deterministic score/rank/id ordering, favourites+watch-later exclusions, truthful seed-grounded reasons; cache keyed by household revision + candidate version, 15min TTL, no-op stability; degraded serving never blocks Home; capability advertised only when wired; 12 unit + DB integration + handler + catalog tests and a live run on disposable PostgreSQL: cold 5ms / warm 1ms local measurement) | Deterministic ranking, cross-provider IDs, no seeds, invalidation, upstream errors | Remove service + capability advertisement; route 404s |
| [x] M4.2 | M4.1 | Shared Home recommendation row and See all, labelled reason/fallback. VERIFIED 2026-09-10 — see evidence/m4.2-shared-recommendations-ui.md (shared RecommendationRow + RecommendationsAllPage in desktop and phone/browser entries; verbatim server reasons, Popular-picks fallback, degraded/empty/error+Retry truthful states, capability-gated rendering, generation-guarded transport, qualified opaque ids via shared canonical-route helper; 7 view tests; 8 inspected captures at desktop and phone widths with 2 defects fixed in-batch — raw error leak and dev-route fallback — and 1 LOW spacing follow-up) | Cold start, sparse/empty data and reason fixture screenshots; endpoint measurement | Remove Home mount + routes; endpoint stays but unconsumed |
| [ ] M5.1 | M1.4, M4.2 | Package iOS shared UI, integrate validated player and platform config/lifecycle | Xcode build/install, no Node/IPC bundle dependency; origin/trust/permission tests | Keep preview separate; previous desktop remains available |
| [ ] M5.2 | M5.1 | Complete iPhone interaction, native playback and accessibility verification | Real iPhone + simulator captures; VoiceOver, font scaling, keyboard, back, interruption/progress; performance targets | Disable unverified capabilities; do not mark US1 complete |
| [ ] M6.1 | M5.2 | Android shell consumes same UI/client packages with Android adapters | Install; Back/keyboard/sheet stack; insets/TalkBack/text scale; actual playback matrix | Disable Android distribution; preserve shared source |
| [ ] M6.2 | M6.1 | Verify cross-client release and private Radxa deployment | Desktop/iPhone/Android screenshot matrix, shared-component change reaches all builds; ARM64 play/resume/library/recommendation and rollback | Restore prior binaries, retain additive library data |
| [ ] M6.3 | M6.2 | Close feature evidence and update product support status | Map US1–US5 and FR01–FR10 to tests/captures, no unreported blocked gates | Docs correction only; no fabricated pass |

M1.3/M1.4 can remain hardware-blocked while M2–M4 progress after M1.2. M5 cannot. Do not interpret the dependency table as permission to skip an unexplained baseline regression. If a task is too large after code discovery, split it into numbered child tasks with the same behavior/verification/rollback fields before implementation.
